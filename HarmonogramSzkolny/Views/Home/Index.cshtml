@model  Title 
<div class="container">
    <div class="jumbotron">
        @if (Model != null)
        {
            <h3>@Model.MainTitle</h3>
            <h3>@Model.Description</h3>
        }
    </div>
</div>   
